import React from 'react'

const EmbededTypeForm = () => (
	<div className="type-form">
		<p>Type form here...</p>
	</div>
)

// import { ReactTypeformEmbed } from 'react-typeform-embed' when enabling this having internal server error :/
// <ReactTypeformEmbed url={`https://demo.typeform.com/to/FS5ylM`} />

export default EmbededTypeForm
